on: 
  push:
    branches-ignore:
      - "default"

jobs:
  build_dev:
    if: ${{ github.base_ref != 'main' }}
    runs-on: ubuntu-latest
    environment: 
      name: Dev

    steps:
    - uses: actions/checkout@v2
    - name: build
      run: echo "dev build"
      
  build_prod:
    if: ${{ github.base_ref == 'main' }}
    runs-on: ubuntu-latest
    environment: 
      name: Prod

    steps:
    - uses: actions/checkout@v2
    - name: build
      run: echo "prod build"
      
