on: [push]

jobs:
  dbt_build:
    strategy:
      matrix:
        lambdas: ["lambda1", "lambda2"]
    runs-on: ubuntu-20.04
    environment: 
      name: Dev
      
    env:
      LAMBDA: ${{ matrix.lambdas }}

    steps:
    - uses: actions/checkout@v2
    
    - name: lambda
      run: echo ${{ env.LAMBDA }} 

